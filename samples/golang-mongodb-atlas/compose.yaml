version: '3.8'

services:
  meteor:
    build: .
    container_name: meteor
    environment:
      - MONGO_URL=mongodb+srv://Chris:Defang12345@atlascluster.oo6gmyw.mongodb.net/?retryWrites=true&w=majority&appName=AtlasCluster
      - ROOT_URL=http://your-production-domain.com
      - PORT=3000
    ports:
      - "3000:3000"
