version: "3.8"

services:
  app:
    build: ./app
    ports:
      - "1337:1337"
    environment:
      DATABASE_URL: "postgresql://tasklistdb_owner:zPl3KMOmq5jy@ep-autumn-wind-a5fvcs0v.us-east-2.aws.neon.tech/tasklistdb?sslmode=require"
      SESSION_SECRET: "your_session_secret"
    depends_on:
      - db

  db:
    image: postgres:13
    environment:
      POSTGRES_USER: tasklistdb_owner
      POSTGRES_PASSWORD: zPl3KMOmq5jy
      POSTGRES_DB: tasklistdb
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
